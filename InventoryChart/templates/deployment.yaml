apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
spec:
  replicas: 1

  selector:
    matchLabels:
      app: inventory-service

  template:
    metadata:
      labels:
        app: inventory-service

    spec:
      containers:
        - name: inventory-service

          image: "mousama/inventory-service:latest"

          ports:
          - name: http
            containerPort: 8080


          env:
            - name: RABBITMQ_HOST
              value: rabbitmq

            - name: RABBITMQ_USER
              valueFrom:
                  secretKeyRef:
                    name: rabbitmq-secret
                    key:  username

            - name: RABBITMQ_PASSWORD
              valueFrom:
                  secretKeyRef:
                    name: rabbitmq-secret
                    key: password

            - name: ORDER_NOTIFICATION_QUEUE
              value: order.notification.queue

            - name: USER_SERVICE_BASE_URL
              value: http://127.0.0.1:8000
              
            - name: POSTGRES_DB
              value: inventory-db

            - name: DB_SERVICE
              value: postgres

            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres
                  key: POSTGRES_USER

            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres
                  key: POSTGRES_PASSWORD
